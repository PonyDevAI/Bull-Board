# Bull Board - 共享配置示例
# 复制到 $PREFIX/shared/config/.env 并按需修改

# ---------- Control（Fastify API + 状态机 + SQLite + SSE）----------
PORT=3000
SQLITE_PATH={{PREFIX}}/shared/data/bullboard.db

# ---------- Runner（Go 执行器）----------
SQLITE_PATH={{PREFIX}}/shared/data/bullboard.db
ARTIFACTS_DIR={{PREFIX}}/shared/artifacts
RUNNER_ID=runner-1
MAX_CONCURRENCY=1
LEASE_SECONDS=600
API_BASE_URL=http://127.0.0.1:3000
